<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_margin="@dimen/dimen_4"
        android:background="?selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/background_item_background"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:src="@drawable/app_icon"
            app:layout_constraintDimensionRatio="4:3"
            android:scaleType="centerCrop"
            />
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/background_item_check_box"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableStart="@drawable/vd_invisible"
            android:elevation="@dimen/dimen_8"
            android:padding="@dimen/dimen_8"
            app:layout_constraintStart_toStartOf="@id/background_item_background"
            app:layout_constraintBottom_toBottomOf="@id/background_item_background"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>